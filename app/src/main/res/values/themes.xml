<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsRakaminApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewsRakaminApp" parent="Base.Theme.NewsRakaminApp" />

    <!-- Splash Screen Theme -->
    <style name="Theme.NewsRakaminApp.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item> <!-- Background Color -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item> <!-- Splash Logo -->
        <item name="windowSplashScreenAnimationDuration">1000</item> <!-- Duration in ms -->
        <item name="postSplashScreenTheme">@style/Theme.NewsRakaminApp</item> <!-- Main Theme -->
    </style>
</resources>
